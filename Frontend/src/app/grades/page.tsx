'use client';

import GradesPage from '@/Pages/StudentDegree/GradesPage';

// Force dynamic rendering to prevent build-time errors with AuthProvider
export const dynamic = 'force-dynamic';

export default function Page() {
    return <GradesPage />;
}











